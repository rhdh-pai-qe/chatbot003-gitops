apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: chatbot003-database
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: chatbot003-database
  name: chatbot003-database
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    app.kubernetes.io/instance: chatbot003-database
